{{- if .Values.ingress.enabled }}
{{- $fullname := include "llm-stack.fullname" . }}
{{- $domain := .Values.global.domain }}
{{- $tls := .Values.ingress.tls }}
{{- $hasTls := or $tls.secretName $tls.issuer }}
---
# Open WebUI Ingress
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: {{ $fullname }}-webui
  namespace: {{ .Release.Namespace }}
  labels:
    {{- include "llm-stack.labels" . | nindent 4 }}
  {{- if and $tls.issuer (not $tls.secretName) }}
  annotations:
    cert-manager.io/cluster-issuer: {{ $tls.issuer | quote }}
  {{- end }}
spec:
  {{- if .Values.ingress.className }}
  ingressClassName: {{ .Values.ingress.className | quote }}
  {{- end }}
  {{- if $hasTls }}
  tls:
    - hosts:
        - {{ .Values.openWebui.host | default (printf "chat.%s" $domain) | quote }}
      {{- if $tls.secretName }}
      secretName: {{ $tls.secretName | quote }}
      {{- else }}
      secretName: {{ printf "%s-webui-tls" $fullname | quote }}
      {{- end }}
  {{- end }}
  rules:
    - host: {{ .Values.openWebui.host | default (printf "chat.%s" $domain) | quote }}
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: {{ $fullname }}-open-webui
                port:
                  number: 80
{{- if .Values.keycloak.enabled }}
---
# Keycloak Ingress
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: {{ $fullname }}-keycloak
  namespace: {{ .Release.Namespace }}
  labels:
    {{- include "llm-stack.labels" . | nindent 4 }}
  {{- if and $tls.issuer (not $tls.secretName) }}
  annotations:
    cert-manager.io/cluster-issuer: {{ $tls.issuer | quote }}
  {{- end }}
spec:
  {{- if .Values.ingress.className }}
  ingressClassName: {{ .Values.ingress.className | quote }}
  {{- end }}
  {{- if $hasTls }}
  tls:
    - hosts:
        - {{ printf "auth.%s" $domain | quote }}
      {{- if $tls.secretName }}
      secretName: {{ $tls.secretName | quote }}
      {{- else }}
      secretName: {{ printf "%s-keycloak-tls" $fullname | quote }}
      {{- end }}
  {{- end }}
  rules:
    - host: {{ printf "auth.%s" $domain | quote }}
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: {{ $fullname }}-keycloakx-http
                port:
                  number: 80
{{- end }}
{{- end }}
