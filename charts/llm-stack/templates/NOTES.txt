LLM Stack {{ .Chart.Version }} deployed to namespace {{ .Release.Namespace }}.

{{- if .Values.openWebui.enabled }}

Open WebUI:
  kubectl -n {{ .Release.Namespace }} port-forward svc/{{ include "llm-stack.fullname" . }}-open-webui 8080:80
  Then open http://localhost:8080
{{- end }}

{{- if .Values.keycloak.enabled }}

Keycloak admin:
  kubectl -n {{ .Release.Namespace }} port-forward svc/{{ include "llm-stack.fullname" . }}-keycloakx-http 8180:80
  Then open http://localhost:8180
{{- end }}

{{- if .Values.litellm.enabled }}

LiteLLM gateway:
  kubectl -n {{ .Release.Namespace }} port-forward svc/{{ include "llm-stack.fullname" . }}-litellm 4000:4000
{{- end }}
